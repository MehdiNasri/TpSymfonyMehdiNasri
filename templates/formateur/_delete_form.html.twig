<form method="post" action="{{ path('app_formateur_delete', {'id': formateur.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formateur.id) }}">
    <button class="btn">Supprimer</button>
</form>
