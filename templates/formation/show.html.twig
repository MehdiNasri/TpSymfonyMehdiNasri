{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_index') }}">Retour à la liste des formations</a>

    <a href="{{ path('app_formation_edit', {'id': formation.id}) }}">Mofiier</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
